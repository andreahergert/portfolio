import React from 'react';

const styles = {
  header: {
    textAlign: 'center',
  },
};

function Header() {
  return (
    <header >
      <h1 style={styles.header}>Andrea Hergert</h1>
    </header>
  );
}

export default Header;
